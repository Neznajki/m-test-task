# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \App\Tests\Helper\Unit

aspect_mock:
    include_paths:
        - src
    temp_folder:
        "unit_tests"
